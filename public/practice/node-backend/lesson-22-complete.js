export function updateLesson(lessons,id,input){const index=lessons.findIndex(item=>item.id===id);if(index<0)return{status:404,error:'교육자료를 찾을 수 없습니다.'};const title=input.title===undefined?lessons[index].title:String(input.title).trim();if(!title||title.length>100)return{status:400,error:'제목은 1~100자입니다.'};lessons[index]={...lessons[index],title,updatedAt:new Date().toISOString()};return{status:200,data:lessons[index]}}
