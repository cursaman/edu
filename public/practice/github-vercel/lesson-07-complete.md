# 7회차 완성 — Vercel 프로젝트 연결

Vercel 대시보드에서 다음 순서로 진행합니다.

1. **Add New → Project**를 선택합니다.
2. GitHub 저장소를 Import합니다.
3. Framework Preset이 **Vite**인지 확인합니다.
4. Build Command는 `npm run build`, Output Directory는 `dist`로 확인합니다.
5. Deploy 후 배포 로그와 `*.vercel.app` 주소를 기록합니다.

저장소 접근 권한은 필요한 저장소에만 허용합니다.
