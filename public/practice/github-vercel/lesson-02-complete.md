# 2회차 완성 — 상태·추가·커밋

```powershell
git status --short
git diff -- README.md
git add -- README.md
git diff --cached --check
git commit -m "Update learning guide"
git log -1 --oneline
```

파일을 확인한 뒤 필요한 파일만 추가합니다. 커밋은 확인 가능한 작업 묶음에 붙이는 이름표입니다.
