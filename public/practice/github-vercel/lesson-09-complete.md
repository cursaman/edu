# 9회차 완성 — 배포 로그와 오류 해결

1. Vercel **Deployments**에서 실패한 배포를 엽니다.
2. Build Logs의 첫 오류를 찾습니다.
3. 같은 커밋에서 로컬 `npm ci`와 `npm run build`를 실행합니다.
4. Node.js 버전, 환경변수 이름, 빌드 명령과 `dist`를 확인합니다.
5. 한 가지 원인만 수정해 커밋하고 다시 배포합니다.
6. 실패하면 마지막 정상 배포의 **Redeploy**로 임시 복구합니다.
