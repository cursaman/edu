# 12회차 완성 — Pull Request 검토

```powershell
git status
git diff main...HEAD --stat
npm run build
git push -u origin 작업브랜치
```

PR 본문에는 목적, 주요 변경, 테스트 결과, 화면 이미지, 관련 Issue와 미완료 항목을 기록합니다. Files changed에서 비밀값과 불필요한 파일이 없는지 확인한 뒤 병합합니다.
