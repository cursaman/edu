# 1회차 완성 — Git 설치와 저장소

```powershell
git --version
Get-Location
git status
git rev-parse --show-toplevel
```

- `git --version`: 설치된 Git 버전을 확인합니다.
- `Get-Location`: 지금 작업하는 폴더를 확인합니다.
- `git status`: 변경 상태를 읽습니다.
- 마지막 명령의 경로가 의도한 프로젝트인지 반드시 확인합니다.
