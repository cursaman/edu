# React 교육 홈페이지 최종 인수 문서

## 실행과 빌드
`npm install` → `npm run dev` → `npm run build`

## 핵심 기능
프로그램 검색·필터·상세, 강의실 진도, Supabase 관리자 CRUD, 반응형 화면

## 보안
`.env.local` Git 제외, service role 키 브라우저 금지, 공개 테이블 RLS 적용

## 인수 점검
실제 URL·저장소·환경변수 이름·SQL 적용 순서·복구 방법·미완료 항목을 다음 담당자에게 전달합니다.
