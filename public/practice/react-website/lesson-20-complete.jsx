export default function AsyncState({ loading,error,items,retry }) { if(loading)return <p role="status">자료를 불러오는 중입니다.</p>;if(error)return <div role="alert"><p>{error}</p><button onClick={retry}>다시 시도</button></div>;if(!items.length)return <div><strong>결과가 없습니다.</strong><p>다른 검색어를 입력해 보세요.</p></div>;return <section>{items.map(item=><article key={item.id}>{item.title}</article>)}</section> }
