import { useEffect,useState } from 'react'
export default function Shows() { const [shows,setShows]=useState([]);const [error,setError]=useState('');useEffect(()=>{const controller=new AbortController();fetch('https://api.tvmaze.com/search/shows?q=office',{signal:controller.signal}).then(response=>{if(!response.ok)throw new Error(`요청 실패 ${response.status}`);return response.json()}).then(data=>setShows(data.slice(0,3))).catch(reason=>{if(reason.name!=='AbortError')setError(reason.message)});return()=>controller.abort()},[]);if(error)return <p role="alert">{error}</p>;return <section>{shows.map(({show})=><article key={show.id}><h2>{show.name}</h2><p>평점 {show.rating.average||'정보 없음'}</p></article>)}</section> }
