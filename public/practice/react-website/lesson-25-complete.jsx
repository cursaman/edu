import { useState } from 'react'
import { supabase } from './supabaseClient'
export default function ProgramAdmin(){const [form,setForm]=useState({title:'',category:'',level:''});const [message,setMessage]=useState('');async function save(event){event.preventDefault();if(!form.title.trim()||!form.category||!form.level)return setMessage('필수 항목을 확인해 주세요.');const {error}=await supabase.from('edu_programs').insert({...form,title:form.title.trim()});setMessage(error?error.message:'프로그램을 등록했습니다.')}return <form onSubmit={save}><label>제목<input value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><label>분야<input value={form.category} onChange={e=>setForm({...form,category:e.target.value})}/></label><label>난이도<input value={form.level} onChange={e=>setForm({...form,level:e.target.value})}/></label><button>저장</button><p role="status">{message}</p></form>}
