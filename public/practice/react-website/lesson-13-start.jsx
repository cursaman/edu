import { useState } from 'react'
export default function Filters({ programs }) { const [category,setCategory]=useState('all');const [level,setLevel]=useState('all');return <main>{/* 두 조건 */}</main> }
