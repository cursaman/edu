import { useState } from 'react'
export default function Filters({ programs }) { const [category,setCategory]=useState('all');const [level,setLevel]=useState('all');const visible=programs.filter(item=>(category==='all'||item.category===category)&&(level==='all'||item.level===level));const reset=()=>{setCategory('all');setLevel('all')};return <main><select value={category} onChange={e=>setCategory(e.target.value)}><option value="all">전체 분야</option><option>프런트엔드</option></select><select value={level} onChange={e=>setLevel(e.target.value)}><option value="all">전체 난이도</option><option>기초</option></select><button onClick={reset}>초기화</button><p>{visible.length}개</p></main> }
